# Changelog

All notable IncidentSeal changes will be recorded here. The project follows Semantic Versioning once releasable software exists.

## Unreleased

### Added

- Approved product promise, trust boundary, operating authority, and non-goals.
- Portable project control plane and checkpoint `IS-0001` contract.
- Initial threat model, environment inventory, roadmap, evaluation plan, release matrix, and devlog.
- Versioned workflow-manifest, external-approval, CLI-envelope, and run-event schemas.
- RFC 8785 canonicalization vectors and dependency-free contract and mutation validators.
- Frozen host CLI and operator-owned manifest-authority contracts.
- Dependency-free manifest parser, v1 validator, RFC 8785 canonicalizer, and SHA-256 identity implementation.
- Machine-readable `policy lint` and `policy digest` commands with Windows and POSIX checkout launchers.
- Agent-safe repository guidance and 13 manifest/CLI contract tests.
- Read-only `policy status` and `policy diff` commands with distinct MATCH, MISMATCH, MISSING, EXPIRED, and INVALID states.
- External approval custody checks for repository/forbidden overlap, Windows ownership and ACLs, symlinks and reparse points, case ambiguity, and process-environment shadowing.
- Stable exit-77 rejection for agent-facing approval mutation attempts.
- Repeatable 25-scenario, 50-execution fail-closed policy and custody evaluation.
- TTY-only, full-digest operator approval with fixed custody, atomic compare-and-swap replacement, exact superseded-record retention, post-write verification, and rollback.
- Portable Windows and POSIX checkout launchers validated from clean temporary custody, including native-Windows Python path conversion under Git Bash.
- Source-gated, exact-hash, temporary-only Draft 2020-12 meta-schema evaluation with four schemas, eight fixtures, and six locked Python wheel artifacts.
- Clean-copy CLI contract verification covering 38 tests, ten real-surface checks, and the 25-scenario/50-execution fail-closed matrix without Docker or real approval state.
- Public IS-0002 checkpoint verification from an exact credential-free GitHub clone, including project controls, Git object integrity, secret scanning, and full CLI-contract replay.
- Verified annotated checkpoint marker `checkpoint-is-0002` at the exact IS-0002 closure commit.
- Exact linux/amd64 image lock with retained signatures, SLSA/apko/SPDX attestations, offline vulnerability scans, license limits, failed superseded candidates, and conditional runtime constraints for the Dockerfile frontend, PostgreSQL, Node, and Python roles.
- Digest-bound topology, normalized-render, staged-custody, and host-orchestration contracts with dependency-free validation and 12 fail-closed security mutations; no Docker runtime surface was started.
- Real `topology validate` machine CLI, exact locked Compose and copy-only Dockerfile implementation, idempotent migration, standard-library Python and Node runners, stable redacted render digests, and 13 implementation-level fail-closed mutations without an image build or container start.
- Host-owned `topology runtime-probe` build/recovery path with retained exact image IDs and failed-volume evidence; the first real startup exposed and preserved a non-root PostgreSQL volume-ownership failure instead of weakening the `70:70` gate.
- Topology revision 2 with a fourth exact-base copy-only database image, ownership-seeded non-root volume path, exact four-image runtime lock, 14 contract and 14 implementation mutations, and repeated real topology-security probes with volume resume and container/network teardown.
- Real `topology database-probe` CLI with exit-10 product FAIL semantics, idempotent migration, identity, schema, DML, forbidden-DDL, restart-persistence, and teardown checks; its first run retained the shared-bootstrap-superuser least-privilege defect.
- Topology revision 3 with separate `incidentseal_admin` and `incidentseal_runner` roles, revoked public creation, bounded runner DML, inaccessible migration metadata, new exact image IDs, and two repeated database-surface PASS runs.
- Real `topology python-probe` CLI with fixed positive and negative staged fixtures, independently computed receipt and database verification, exact image and container isolation inspection, between-command one-shot cleanup, exit-10 product failure retention, and two repeated real Python-surface PASS runs.
- Real `topology node-probe` CLI with exact Node output and PostgreSQL verification, retained Python-row consistency, shared canonical input identity, distinct language result identity, malformed-input rejection, hardened isolation, and two repeated real Node-surface PASS runs.
- Disposable `topology reliability-probe` with a three-volume protection lock, fresh bootstrap, both real runners, distinct FAIL/INVALID/failed/cancelled observations, post-failure recovery, restart persistence, orphan detection, repeatability, verified disposable teardown, and exact credential-free public-clone replay.
- Verified public hardened-topology checkpoint `checkpoint-is-0003` at exact commit `a0c05070dd1d147aecae6b4ed686440414a3aa27` with independently verified annotated tag object `28eea260147265e7dd0328dcd072e134586a2ff0`.
- Frozen portable receipt and independent-verification contracts with exact RFC 8785 receipt/event/link identities, raw artifact digests, approved-workflow versus platform authority separation, external expected-digest requirement, full Draft 2020-12 validation, and 14 fail-closed mutations.
- Dependency-free `receipt materialize` and `receipt verify` commands with atomic content-addressed custody, idempotent reuse, read-only offline verification, runtime source locking, state-separated negative evidence, and 12 implementation-level fail-closed mutations.
- Frozen append-only event-journal record and result contracts with domain-separated deterministic idempotency, contiguous link and authority rules, exact replay, explicit stale/superseded retention, full Draft 2020-12 validation, and 16 fail-closed mutations.
- Verified immutable PostgreSQL event journal with per-run transaction serialization, exact-byte replay, unique conflict gates, mutation-denial triggers, read-only canonical `run events` JSONL, real stale/superseded exits, restart persistence, least privilege, protected disposable custody, and exact credential-free public replay.
- Publicly reproduced fenced interruption-recovery contract with content-addressed observations and decisions, exact lease and runtime ownership, safe replay and committed-effect continuation, distinct cancellation/failure/stale handling, ambiguous deferral, conflicting-effect `FAIL`, 12 frozen cases, and 20 fail-closed mutations.
- Host-only fixed synthetic recovery implementation with separate PostgreSQL recovery fencing, durable atomic pending custody, exact Docker ownership and hardening checks, stop-then-reobserve, idempotent replay, cancellation/failure/stale terminals, crash-after-evidence resume, concurrent-recoverer exclusion, restart persistence, runner privilege denial, 17 implementation mutations, protected-volume identity, and full disposable teardown.
- Exact credential-free public recovery replay with 74 tests, every cross-surface validator and mutation suite, offline exact-wheel schema validation, strict Git integrity, zero high-confidence secret hits, unchanged missing approval, 15 real recovery checks, and complete disposable teardown.
- Publicly reproduced content-addressed PostgreSQL logical backup and clean-restore contract with exact archive and normalized TOC identity, measured roles, five negative privileges, protected-volume identity, teardown, 18 fail-closed mutations, and 80 tests.
- Fixed host-only backup/restore implementation with source write fencing, durable exact custom archive custody, unsafe-TOC rejection, clean distinct restore, migration-owned role hardening, exact state equivalence, restart persistence, five denied runner privileges, protected-volume preservation, complete teardown, 21 implementation mutations, and 86 tests.
- Exact credential-free public backup/restore implementation replay at commit `f8c2526389ea73c157f535c2d6651ba86b8169ac`, including every cross-surface suite, offline schema validation, strict Git integrity, zero secret matches, missing approval, ten real checks, unchanged protected volumes, and complete disposable/archive teardown.
- Frozen runtime-free integrated receipt and recovery matrix contract with 20 state-separated cases, five isolated host-owned stages, six existing locked commands, two complete repetitions, per-receipt raw archive identity, stable semantic comparisons, mandatory protected-volume and teardown gates, 28 fail-closed mutations, and full Draft 2020-12 validation.
- Exact credential-free public integrated-contract replay at commit `6a27a5fdcc641fa090f70e9c32615c58b6920b0b`, including 93 tests, every inherited validator and mutation suite, the 25-scenario/50-execution authority matrix, 13 clean-copy CLI checks, offline exact-wheel schema validation, strict Git integrity, zero secret matches, missing approval, zero product runtime custody, unchanged protected volumes, and retained operator-pause evidence.
- Separately locked argument-free integrated host harness with no new child CLI authority, 101 tests, 39 fail-closed implementation mutations, two complete five-stage local real cycles, forty state observations, per-receipt raw archive identity, stable cross-cycle semantics, exact protected-volume preservation, and complete disposable teardown.
- Exact credential-free public integrated implementation replay at commit `a06ab530dd4ae9d14372d797142a9910b82b4d08`, including every inherited gate, both complete real cycles, forty state observations, four total local/public raw archive receipts, missing approval, unchanged protected volumes, and complete teardown.
- Verified public evidence-and-recovery checkpoint `checkpoint-is-0004` at exact commit `25328dacef4d9283090bed809db75b33f613829b`, with a clean-clone checkpoint matrix, another two-cycle real replay, and independently fetched annotated tag object `60b467a7970a6fb6b5e80dcdc4dd283ab80b0acf`.
- Opened IS-0005 dashboard and evaluation governance from `checkpoint-is-0004`, with a contract-first loopback-only read-only surface, exact record projection, nine-state scenario corpus, rendered QA, and repeated measurement plan that grants no Docker, approval, workflow, external-network, or repository-write authority.
- Added the local runtime-free IS5-U01 dashboard contract candidate: two closed schemas, seven exact checkpoint source bindings, eight exits, nine adversarial scenarios repeated three times, explicit visual and evaluation gates, 37 fail-closed mutations, 111 passing tests, and offline Draft 2020-12 validation without starting a server, browser, or Docker runtime.
- Publicly reproduced the IS5-U01 dashboard contract from exact credential-free commit `2e22804aef49071ef42611357eaa4a7f4e0f384e`, including all tests and mutations, offline schema evaluation, Git and secret integrity, missing approval, exact protected-volume custody, and zero server, browser, or product runtime.
- Added the local locked IS5-U02 dashboard implementation candidate: a separate dependency-free loopback launcher, exact source-bound in-memory projection, five fixed routes, local assets, nine internal scenario views, eight defensive headers, exact Host enforcement, 29 fail-closed implementation mutations, and a 25-request real Windows launcher replay with complete teardown.
- Publicly reproduced the exact IS5-U02 implementation at commit `6a5d1cf8fe359b6eaa2098d7bbe00092d56034a2`, including byte-identical HTTP responses, the full attack-denial matrix, 125 tests, all contract and implementation mutations, offline schema validation, Git/secret integrity, missing approval before and after, exact retained-volume custody, and zero process residue.
- Verified and publicly reproduced the real IS5-U03 rendered dashboard at exact candidate commit `46639491f2591649091d04cdc246f79a8c591e18`: one desktop and nine mobile states, eleven retained screenshots, semantic and keyboard accessibility, responsive layout, contrast, touch targets, reduced motion, local-only browser requests, calibrated claim withholding, 129 tests, all 37/29/28 mutation suites, unchanged authority and Docker custody, complete process teardown, and distinctly retained FAIL and INVALID evidence.
- Frozen PostgreSQL logical backup and clean-restore receipt contract with exact custom archive and normalized TOC identities, fixed dump/restore commands, distinct clean target custody, measured cluster-global roles, post-restore migration hardening, schema/journal/result/role equivalence, five negative privilege checks, 18 fail-closed mutations, and full Draft 2020-12 validation without runtime.
- Exact credential-free public backup/restore contract replay with 80 tests, every cross-surface validator and mutation suite, offline exact-wheel schema validation, strict Git integrity, zero high-confidence secret hits, unchanged missing approval, unchanged protected volumes, and zero Docker or PostgreSQL runtime execution.
